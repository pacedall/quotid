{
  "name": "quotid-era",
  "version": "0.1.0",
  "description": "Quotid · Era — a daily timeline puzzle. Deterministic daily engine, server-side streaks, optional accounts.",
  "main": "server.js",
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node server.js",
    "init-db": "node scripts/init-db.js",
    "reminders": "node scripts/send-reminders.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.12.0",
    "resend": "^3.4.0"
  }
}
